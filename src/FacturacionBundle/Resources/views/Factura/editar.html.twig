{% extends '::base.html.twig' %}
{% block content %}
    <section class="content"> 
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Editar Factura</h1>
            </div>
        </div>
        <div class="panel panel-default">
            <div class="panel-heading">
                Editar citas
            </div>       
            <div class="panel-body">
                <form width="100%" class="table table-striped table-bordered table-hover" id="citas">

                    {{ form_start(edit_form) }}
                    {{ form_widget(edit_form) }}
                    <input type="submit" value="Edit" />
                    {{ form_end(edit_form) }}
                </form>

            </div>

        </div>

        <ul>
            <li>
                <a href="{{ path('factura_lista') }}">volver</a>
            </li>
            <li>

            </li>
        </ul>
    </section>

{% endblock %}
