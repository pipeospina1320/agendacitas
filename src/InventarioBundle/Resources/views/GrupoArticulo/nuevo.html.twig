{% extends '::base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Nuevo Articulo</h1>
                </div>
            </div>
            <div class="panel panel-default">
                <div class="panel-heading">
                    Crear articulo
                </div>       
                <div class="panel-body">
   

                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <input type="submit" value="Create" />
                        {{ form_end(form) }}
                 
                </div>

            </div>
            <ul>
                <a href="{{ path('citas_lista') }}">Back to the list</a>
            </ul>
        </div>
    </div>


{% endblock %}
