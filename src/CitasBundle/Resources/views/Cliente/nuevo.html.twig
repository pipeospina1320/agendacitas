{% extends '::base.html.twig' %}

{% block body %}
    <h1>Cliente creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('cliente_index') }}">Volver</a>
        </li>
    </ul>
{% endblock %}
