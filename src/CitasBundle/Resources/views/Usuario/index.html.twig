{% extends 'base.html.twig' %}

{% block body %}
    <h1>Usuarios list</h1>

    <table>
        <thead>
            <tr>
                <th>Codigoclientepk</th>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for usuario in usuarios %}
            <tr>
                <td><a href="{{ path('usuario_show', { 'codigoClientePk': usuario.codigoClientePk }) }}">{{ usuario.codigoClientePk }}</a></td>
                <td>{{ usuario.nombre }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('usuario_show', { 'codigoClientePk': usuario.codigoClientePk }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('usuario_edit', { 'codigoClientePk': usuario.codigoClientePk }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('usuario_new') }}">Create a new usuario</a>
        </li>
    </ul>
{% endblock %}
